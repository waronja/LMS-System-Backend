<%= form_with(model: lesson) do |form| %>
  <% if lesson.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(lesson.errors.count, "error") %> prohibited this lesson from being saved:</h2>

      <ul>
        <% lesson.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :chat_id, style: "display: block" %>
    <%= form.number_field :chat_id %>
  </div>

  <div>
    <%= form.label :assessment_id, style: "display: block" %>
    <%= form.number_field :assessment_id %>
  </div>

  <div>
    <%= form.label :phase_id, style: "display: block" %>
    <%= form.number_field :phase_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
